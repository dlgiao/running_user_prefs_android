<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kmmania.runninguserpreferences">

    <application
        android:name=".application.RunningUserPrefApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RunningUserPreferences">
        <activity android:name=".views.WeightActivity"></activity>
        <activity android:name=".views.HeightActivity" />
        <activity android:name=".views.MasActivity" />
        <activity android:name=".views.DobActivity" />
        <activity android:name=".views.GenderActivity" />
        <activity android:name=".views.MeasuringSystemActivity" />
        <activity android:name=".views.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>