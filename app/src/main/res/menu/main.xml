<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/action_help"
        android:title="@string/help"
        android:orderInCategory="100"
        app:showAsAction="never" />
    <item android:id="@+id/nav_user_prefs"
        android:title="@string/user_prefs"
        android:contentDescription="@string/content_description_user_prefs" />
    <item android:id="@+id/nav_info"
        android:title="@string/info"
        android:contentDescription="@string/content_description_info" />
</menu>